This is the Back-end API for ShoppingCart Project

Its main purpose is to GET products and CRUD cart data.

It uses cookies to store the cart data.

This API follows Clean Architecture

It's been built with considerations of different users(guests and users). For now the website functionality supports just a guest.